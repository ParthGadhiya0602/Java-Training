# Dev profile - overrides base application.properties values.
# Spring Boot merges: base + profile-specific, profile wins on conflict.

app.database.url=jdbc:h2:mem:devdb
app.database.pool-size=5

info.app.environment=development
