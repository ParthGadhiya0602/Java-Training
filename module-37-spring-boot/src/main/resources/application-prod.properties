# Prod profile — stricter pool, external DB URL.
# In a real app this URL would come from an environment variable or Vault,
# not be hardcoded here. Shown as a string only — no actual connection made.

app.database.url=jdbc:postgresql://prod-host:5432/proddb
app.database.pool-size=50

info.app.environment=production
