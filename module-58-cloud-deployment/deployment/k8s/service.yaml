apiVersion: v1
kind: Service
metadata:
  name: cloud-deployment-demo
spec:
  selector:
    app: cloud-deployment-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
