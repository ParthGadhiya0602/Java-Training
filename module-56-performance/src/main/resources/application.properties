# Spring Boot 3.2+ — use virtual threads for all HTTP request handling.
# Tomcat switches from its platform-thread pool to a virtual-thread-per-request model.
# No code changes required; the dispatcher and filters run on virtual threads automatically.
spring.threads.virtual.enabled=true
