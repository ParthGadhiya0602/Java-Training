spring.application.name=spring-security-demo

# JWT configuration — secret must be at least 32 bytes for HMAC-SHA-256
jwt.secret=SpringSecurityModuleTestSecretKeyFor256BitHMAC
jwt.expiration-ms=3600000
