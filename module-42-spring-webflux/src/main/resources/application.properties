spring.r2dbc.url=r2dbc:h2:mem:///reactivedb
spring.r2dbc.username=sa
spring.r2dbc.password=
# Apply schema.sql on every startup (safe because it uses CREATE TABLE IF NOT EXISTS)
spring.sql.init.mode=always
