# Embedded MongoDB — flapdoodle requires an explicit version choice.
# The binary is downloaded once and cached.  Any supported MongoDB version works;
# 6.0.6 is an LTS release well-tested with the embedded library.
de.flapdoodle.mongodb.embedded.version=6.0.6
