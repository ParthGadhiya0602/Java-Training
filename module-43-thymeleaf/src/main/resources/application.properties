spring.datasource.url=jdbc:h2:mem:thymeleafdb;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
# Disable template caching — reload templates on every request (dev only)
spring.thymeleaf.cache=false
